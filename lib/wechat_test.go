package lib

import (
	"testing"
)

func TestWechatInfo(t *testing.T) {
	// assert := assert.New(t)
	// backups, err := LookupBackupDirectories()
	// assert.Nil(err)
	// if len(backups) > 0 {
	// 	t := NewWechatTraverser(backups[0])
	// 	users := t.GetUserList()
	// 	assert.NotEmpty(users)
	// }
}
